Project to create a demo of a sustainability solution for my early careers programme.

This project will show how, with additional work, a RAG solution could be used to meet sustainability goals.

To start the backend, install the relevant backend packages in your venv and then run: unicorn main:app --reload

To start the frontend run npm start
